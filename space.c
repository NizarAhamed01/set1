#include<stdio.h>
#include<conio.h>
void main()
{
char str[50];
int i,count=0;
clrscr();
printf("\n Enter The String = ");
gets(str);
for(i=0;str[i];i++)
if(str{i]=='')
count++;
printf("\n Total Spaces:%d",count);
getch();
}
